{% extends 'base.html.twig' %}

{% block title %}Mes certifications{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-primary text-center mb-4">🏆 Mes certifications</h1>

    {% if certifications|length > 0 %}
        <div class="row g-3 mb-4">
            {% for course in certifications %}
                <div class="col-12 col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title">{{ course.title }}</h5>
                            <p class="card-text text-success fw-bold">✅ Certifié</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="alert alert-info text-center">
            Vous n'avez encore validé aucun cours.
        </div>
    {% endif %}

    <div class="text-center mt-4">
        <a href="{{ path('front_themes') }}" class="btn btn-secondary">← Retour aux thèmes</a>
    </div>
</div>
{% endblock %}
