{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-primary text-center mb-4">📚 Dashboard de {{ user.email }}</h1>

    {% if themes is defined and themes|length > 0 %}
        {% for theme in themes %}
            {% set accessibleCourses = theme.accessibleCourses|default([]) %}

            {% if accessibleCourses|length > 0 %}
                <h2 class="mt-4">{{ theme.name }}</h2>
                <div class="row g-3">
                    {% for course in accessibleCourses %}
                        <div class="col-12 col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body d-flex flex-column justify-content-between">
                                    <h5 class="card-title">{{ course.title }}</h5>
                                    <p class="card-text"><strong>Prix :</strong> {{ course.price }} €</p>
                                    <a href="{{ path('front_course_purchase', {'id': course.id}) }}" class="btn btn-primary w-100 mt-3">
                                        Acheter le cours
                                    </a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                <div class="alert alert-info text-center mt-2">
                    Aucun cours disponible pour le thème "{{ theme.name }}".
                </div>
            {% endif %}
        {% endfor %}
    {% else %}
        <div class="alert alert-info text-center">
            Aucun thème disponible pour le moment.
        </div>
    {% endif %}

    <div class="mt-4 text-center">
        <a href="{{ path('front_themes') }}" class="btn btn-secondary">← Retour aux thèmes</a>
        <a href="{{ path('front_certifications') }}" class="btn btn-success ms-2">🏆 Mes Certifications</a>
    </div>
</div>
{% endblock %}
