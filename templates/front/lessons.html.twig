{% extends 'base.html.twig' %}

{% block title %}Leçon: {{ lesson.title }}{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="card shadow-sm rounded-3 p-4">
        <h1 class="h4 text-primary mb-3">📘 Leçon : {{ lesson.title }}</h1>

        <p><strong>Prix :</strong> {{ lesson.price }} €</p>

        <!-- Lesson content -->
        <div class="lesson-content mb-4">
            <p>{{ lesson.content ?: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Curabitur euismod, nisl vel tincidunt facilisis, neque quam laoreet purus, a fermentum nulla erat id ex.' }}</p>
        </div>

        <!-- Embedded video if available -->
        {% if lesson.videoUrl %}
            <div class="mb-4 ratio ratio-16x9">
                <iframe src="{{ lesson.videoUrl }}" frameborder="0" allowfullscreen></iframe>
            </div>
        {% endif %}

        <!-- Validation button or indicator -->
        <div class="mb-4">
            {% if hasValidated %}
                <p class="text-success fw-bold">✅ Cette leçon est validée</p>
            {% else %}
                <form action="{{ path('front_lesson_validate', {'id': lesson.id}) }}" method="post" style="display:inline;">
                    <button type="submit" class="btn btn-success">Valider la leçon</button>
                </form>
            {% endif %}
        </div>

        <!-- Link back to parent course -->

        {% if lesson.course.theme %}
            <a href="{{ path('front_theme_courses', {'themeId': lesson.course.theme.id}) }}" class="btn btn-secondary">← Retour au thème</a>
        {% else %}
            <a href="{{ path('front_themes') }}" class="btn btn-secondary">← Retour aux thèmes</a>
        {% endif %}

    </div>
</div>
{% endblock %}
