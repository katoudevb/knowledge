{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-primary text-center mb-4">🎓 Bienvenue sur la plateforme de formation</h1>
    <p class="text-center mb-4">Choisissez un thème pour explorer les cours disponibles :</p>

    {% if themes is not empty %}
        <div class="row g-3">
            {% for theme in themes %}
                <div class="col-12 col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title">{{ theme.name }}</h5>
                            <p class="card-text">{{ theme.description ?: 'Aucune description disponible.' }}</p>
                            <a href="{{ path('front_theme_courses', {'themeId': theme.id}) }}" class="btn btn-primary mt-3 w-100">
                                Voir les cours
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="alert alert-info text-center">
            Aucun thème disponible pour le moment.
        </div>
    {% endif %}
</div>
{% endblock %}
