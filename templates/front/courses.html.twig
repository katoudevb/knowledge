{% extends 'base.html.twig' %}

{% block title %}Cours: {{ course.title }}{% endblock %}

{% block body %}
<div class="container my-5">

    <div class="card shadow-sm rounded-3 mb-4 p-4">
        <h1 class="h4 text-primary mb-3">📖 Cours : {{ course.title }}</h1>
        <p>{{ course.description }}</p>
        <a href="{{ path('front_purchase', {'type': 'course', 'id': course.id}) }}" class="btn btn-primary w-100 mt-3">
            Acheter le cours
        </a>
    </div>

    <h2 class="mb-3">Leçons</h2>
    {% if course.lessons|length > 0 %}
        <div class="row g-3 mb-4">
            {% for lesson in course.lessons %}
                <div class="col-12 col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title">{{ lesson.title }}</h5>
                            <a href="{{ path('front_lessons', {'id': lesson.id}) }}" class="btn btn-secondary mt-3 w-100">
                                Voir la leçon
                            </a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="alert alert-info text-center">
            Aucune leçon disponible.
        </div>
    {% endif %}

    <section>
        <h3>Description :</h3>
        <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.</p>
    </section>

</div>
{% endblock %}
