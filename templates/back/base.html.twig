<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Backoffice{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-2 d-none d-md-block bg-light sidebar">
                <div class="position-sticky pt-3">
                    <ul class="nav flex-column">
                        <li class="nav-item"><a class="nav-link" href="{{ path('admin_course_index') }}">📚 Cours</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('admin_lesson_index') }}">📝 Leçons</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('admin_theme_index') }}">🎨 Thèmes</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('admin_user_index') }}">👤 Utilisateurs</a></li>
                    </ul>
                </div>
            </nav>

            <!-- Contenu -->
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <h1 class="mt-4">{% block page_title %}Dashboard{% endblock %}</h1>
                <div class="content">
                    {% block body %}{% endblock %}
                </div>
            </main>
        </div>
    </div>
</body>
</html>
